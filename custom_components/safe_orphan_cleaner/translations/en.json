{
  "services": {
    "scan": {
      "name": "Scan",
      "description": "Scans the entity registry for entries flagged as orphaned by Home Assistant itself (orphaned_timestamp set). Writes two ready-to-run bash scripts under <config>/safe_orphan_cleaner/ - safe_orphans_remove.sh and dangerous_orphans_remove.sh, each line already a full removal call - and returns counts plus the path to each script.",
      "fields": {}
    }
  }
}