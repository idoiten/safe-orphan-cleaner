{
  "services": {
    "scan": {
      "name": "Scan",
      "description": "Scans the entity registry for entries flagged as orphaned by Home Assistant itself (orphaned_timestamp set). Writes two files under <config>/safe_orphan_cleaner/ - safe_orphans_remove.txt and dangerous_orphans_remove.txt, one internal registry_id per line - and returns counts plus a ready shell command for each file.",
      "fields": {}
    }
  }
}
